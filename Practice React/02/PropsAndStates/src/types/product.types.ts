export type TCurrencyProductCards = {
  name: string;
  price: string;
  description: string;
  category: string;
  initialStock: number;
  imageURL: string;
  alt: string;
};

export type TDisplayNumber = {
  number: number;
};
